# ADR 0006. 저장소는 DuckDB 단일 파일

- 날짜: 2026-09-04
- 상태: 확정

## 맥락
1인 개발, 분석 쿼리 위주, 데이터 규모는 수백만 행 이하.

## 결정
`data/sabior.duckdb` 단일 파일 + Parquet 내보내기. 외래키 제약은 두지 않고(재적재 편의) 정합성은 테스트로 검사한다. 시점 기준 조회는 `*_as_of(d)` 테이블 매크로로만 한다.

## 결과
서버 운영이 없다. PostgreSQL 은 다중 사용자 서비스가 필요해질 때 재검토한다.
